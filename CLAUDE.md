# Wiki-Brain 开发上下文

## 项目概述

Wiki-Brain 是一个知识检索系统，核心流程：文件导入 → KU/KP 提取 → 检索 → 回答 → 质量追踪 → 学习报告。

**MVP 已完成并通过测试**（internal/foundation、source、unit、retrieval、answer、trace、study、session 均已实现）。当前处于 **V1（能力提升版）** 实现阶段，目标：让检索信号能转化为长期记忆（ActivationLink、Wiki 初版），并反过来改变检索行为（激活路径优先、LLM 调用减少）。

实现文档：MVP 在 `docs/impl/mvp/`，V1 在 `docs/impl/v1/`，V2（暂不实现）在 `docs/impl/v2/`。**所有设计决策以文档为准，不得自行发明**；文档本身如有歧义或与下方"V1 关键设计决策"不一致，先向用户确认，不要edge-case 猜测实现。

## 实现顺序（严格按序，不得跳跃）

**MVP**（已完成）：1. Foundation → 2. Source → 3. Unit → 4. Retrieval → 5. Answer → 6. Trace → 7. Study → 8. Page

**V1**（进行中）：1. lifecycle → 2. activation → 3. trace（扩展） → 4. study（扩展） → 5. retrieval（扩展） → 6. evidence → 7. kpn（跨 Source） → 8. wiki → 9. page（扩展）

每个模块实现完成、`go test ./...` 全绿后，再开始下一个，不要并行推进多个模块。

## V1 关键设计决策（近期定稿，实现时务必对照 docs/impl/v1/ 精确核对，不要凭旧印象或直觉简化/扩展）

- **KPN 关系类型只有 2 种**：`related` / `contradicts`，`direction` 恒为 `bidirectional`（MVP 的 `internal/unit/service.go` 已是此实现）。V1 跨 Source 匹配（`kpn.md`）保持同样的 2 种，不要按早期设计文档的 5 种类型实现。
- **KU/KP lifecycle 只有 3 种状态**：`current` / `superseded` / `deprecated`，没有 candidate/needs_verification/conflicted/historical/retracted（详见 `docs/design/lifecycle.md` 第 2 节的场景推导）。`activation_links` 自己有独立状态机（candidate/verified/weakened/deprecated，见 `activation.md`），与 KU/KP 的 lifecycle 是两套不同状态，不要混用。
- **Source 重新上传用 Shadow Source 机制**：新文件先在隐藏的影子 Source（`sources.shadow_of` 指向目标 source_id）里走完全正常的 `source_process → unit_extract`（不改动该链路一行代码），全程不影响、不暴露原 Source；只有影子处理全部成功（含 KPN、Concept 匹配）后，才在一个事务里把影子内容的 `source_id` 改写为目标 source_id、原内容标记 `superseded`、影子行删除。创建影子时要跳过对目标 source_id 自身的文件名去重检查。影子失败直接丢弃，不需要回滚代码；提供 `POST /sources/:id/reupload/retry` 复用已有的 `POST /sources/:shadow_id/retry` 续跑逻辑。Retrieval 的 Domain 预过滤、Source 语义过滤要排除 `shadow_of IS NOT NULL` 的行。详见 `docs/impl/v1/lifecycle.md`。
- **Study 报告已有 `kpn_citation_rate`**：retrieval 层 `Evidence.origin` 字段、trace 层 `kpn_cited_count`/`cited_count`、study 层聚合已实现，V1 扩展 traces 表和报告结构时不要破坏这条链路。
- **Wiki 单层化改判（2026-08-18 定案，取代此前"两层架构"的全部口径）**：设计依据 `docs/design/wiki-single-tier-revision.md`，取代 `docs/design/wiki.md`「Wiki 页面类型」「主题：从真实使用中识别」「二阶编译的准入」「页面关系只有三种，层级由 contains 承载」等章节，代码已实际改造完成（`internal/wiki/subgraph.go` 等）。Wiki 只有一种页面（沿用「主题页」这个既有 `page_type` 名字，**不再有「概念页」**），编译单层完成，一次 `POST /wiki/compile`（body `{entry_ids: [...]}`）人工指定一个或多个 Concept/Fact 词条直接产出一份成品页面——**不再有 Study 驱动的自动主题候选识别、qualifying 自动标记 `needs_recompile`**（`buildWikiCandidates`、`topic_cluster_min_questions`/`topic_cluster_min_days_active`、`ScanForNewQualifyingKP` 等自动扫描均已删除，`MarkNeedsRecompile` 本身保留供人工重编译场景使用）。编译材料从"整块塞给 LLM"改为围绕 `entry_ids` 做结构化 **Core/Context/Conflict 子图展开**（`buildKnowledgeSubgraph`）：Core = 每个 entry 直属 KP（`lifecycle=current`），`entry.kind=fact` 且有 `parent_entry_id` 时父 Concept 的 Core 一并纳入并标记为 `SubgraphRoleCoreParentBackground`（背景，非本页核心，不设大小上限）；Context = Core 中 KP 的一跳 `related`；Conflict = Core 中 KP 的一跳 `contradicts`（只展开一跳，不递归）。citation 白名单 = 该 Subgraph 覆盖的全部 `point_id`，正文仍标注 `point_id`，回链「Wiki → entry → KP → KU → source_ref」不变。`wiki_page_relations` 只保留 `related`/`contradicts`（已发布页面之间，程序从 entries 层面的 KPN 派生，不调 LLM）——**`contains` 关系已整体废弃**，不再有「主题页聚合概念页」这回事，`member_roles`/`skeleton_point_ids`/`topic_decompose_signal` 等依赖二阶聚合的机制均不存在。已发布的旧概念页/主题页/`wiki_page_relations` 的 `contains` 行按定案已直接删除，不做迁移。
- **Wiki 编译不是全自动的**：编译/重编译永远需要人工在 `POST /wiki/compile` 中显式指定 `entry_ids` 后才执行，不要做成流水线自动编译。
- **写作编辑落在派生草稿上，不改页面**：`wiki_drafts` 记录来源 `page_id` + `source_revision_id`，人工在草稿上自由改写（不做 citation/结构校验）；`wiki_pages.content` 仍然只由编译产生，**不存在任何 draft → page 的写回接口**。草稿内容要长期沉淀就走 `POST /sources` 正常导入链路回流。
- **检索匹配 Wiki 页面已改为 LLM 识别，不是四元组精确匹配**（取代此前 `matchFourTupleEntry` 口径，该函数已删除，`internal/wiki/service.go:1601` 留有 `// old four-tuple exact-match entry (matchFourTupleEntry, removed).` 注释）：`internal/wiki/service.go` 的 `TryDirectAnswer` → `gatherDirectAnswerCandidates` 合并三路信号（LLM 概念/事实识别 `RecognizeEntries` 命中已发布 entry → 查其页面；Bleve 词法索引按 `retrieval.wiki_min_score` 过滤；归一化文本子串匹配），逐个候选（上限 `retrieval.wiki_max_candidates`）跑 `answerFromPage` 直答验证，第一个 `sufficient=true` 即命中；命中后的分类/证据充分性验证流程与旧口径一致，只是"怎么找到候选页面"这一步换成了 LLM 识别，多计入一次快路径 LLM 调用预算。
- **Wiki 直答与 ActivationLink 快路径改为并行发起，ActivationLink 优先（2026-08-19 定案，取代此前"Wiki 直答第0层先跑、未命中才跑激活层"的串行口径）**：Wiki 直答的 Concept/Fact 识别要调 LLM，比纯程序、免费的 ActivationLink Match 慢得多，串行会让本该被激活层秒回的问题被 Wiki 的 LLM 调用拖慢延迟下限。`internal/retrieval/service.go` 的 `RetrieveWithProgress` 用两个 goroutine 并行发起 `tryWikiAnswer` 与 `tryFastPath`，`sync.WaitGroup` 等两者都返回后按优先级择一：ActivationLink 命中（含 exploring 档中选）优先于 Wiki 直答命中；Wiki 命中而激活层未命中则用 Wiki 结果；都未命中落慢路径。这意味着即便最终用的是 ActivationLink 结果，Wiki 直答那次 LLM 调用仍会照常发生并被丢弃——用一次可能浪费的调用换延迟下限，是这次改判的核心权衡，不要因为"结果没用上"误以为是 bug 而改回按需短路。详见 `docs/impl/v1/retrieval.md`「检索总流程」2026-08-19 编注。
- **Wiki 草稿回流必须防自指**：草稿导入时打 `sources.origin='wiki_draft'` + `origin_page_id`；KPN 匹配要剔除来源页面已引用过的 KP（复印件不是关系），且这些边不计入 qualifying / 页面关系派生 / 主题候选核验统计。否则关系边、连贯度、可编译主题数会虚增，系统在自己身上打转还显得在"增长"。回流 KP 与其他知识的关系照常建立。
- **主题候选从真实提问识别，不从已发布页面事后聚类**（2026-08-03 修订，设计依据 `docs/design/wiki.md`「主题：从真实使用中识别，而不是从已发布词条事后聚类」，取代此前"对已发布概念页求连通分量"的口径）：Study 对 `traces` 按归一化四元组 `(subject, intent, audience, constraint_text)` 分组，分组同时满足 `distinct_question_count ≥ wiki.topic_cluster_min_questions`、`days_active ≥ wiki.topic_cluster_min_days_active` 才是主题候选——此刻不涉及任何知识点，与材料侧是否已有已发布页面无关。候选范围确定后才在其中检索知识点、筛出**主题范围材料**（`lifecycle=current`，**不**要求 verified——2026-08-04：手动/主题范围先出草稿，verified 留给 Study 推荐路径的一阶材料 qualifying、ready、二阶可靠度与正式发布；2026-08-07 修订：人工手动触发的一阶材料 qualifying 同样不再要求 verified，只有 Study 推荐路径（`result_id` 非空）保留 verified 门槛——「ready」判定信号与发布 selfcheck 质量门槛不受此影响，仍然不变，详见 `docs/impl/v1/wiki.md` 步骤 2「人工指定主题手动编译」）、按 entry_id 分组：已发布概念页直接复用，未发布但满足概念级 ready 判定的分组随批推进一阶编译。二阶编译准入回答两件事：**关联**（成员概念页之间 related 连接为主导、contradicts 不反客为主）与**整体可靠度**（候选范围全部知识点的 verified 覆盖占比达标），不再对页面图求连通分量、不设候选创建时的成员数硬性区间。`wiki_pages.uncovered_points`（current 但无 verified 链接的 KP）**只作字段、不进正文**——正文四节/五节结构与 citation 白名单校验一条不动。`internal/study/`、`internal/wiki/` 中的连通分量实现尚未跟进这次修订，编码前应以 `docs/impl/v1/wiki.md` 步骤 8 为准重新核对，不要照搬旧实现。
- **ActivationLink 晋升默认自动，Wiki 材料准入单独加严（2026-08-11 修订，取代此前"晋升默认需人工确认"的口径）**：`study.auto_promote` 默认改为 `true`——理由是 verified 目前唯一直接解锁的高风险动作是 Retrieval 快路径，而快路径在生成答案前必经 `fast_path_verify`（见 `retrieval.md` 步骤 2a），误晋升的链接答不出来会回落慢路径，不会把错误答案直接送给用户，原先"默认人工确认"要防的风险已经被这道查询时的门槛结构性兜住。但 verified 曾经隐含的另一层含义——"人工看过、值得信赖到可以进入 Wiki 材料池"——不再成立：`wiki.md` 步骤 3 的 qualifying 判据（仅限 Study 推荐路径，`result_id` 非空）改为 verified ActivationLink **加上**一道新的独立关卡 `wiki_material_confirm`（`study.md` 步骤 6，`learning_results(action=wiki_material_confirm, object_type=knowledge_point)`），**始终需要人工确认，没有 auto 开关**——真正的准入判据是这道人工确认本身。确认队列有一道比 ActivationLink 晋升更高的统计门槛（`wiki.qualifying_confirm_success_min` / `qualifying_confirm_distinct_min`），**但这只是给候选排优先级、减轻人工审阅负担，不是又一层可靠性判断**（2026-08-11 定案，见 `docs/design/wiki.md`「ActivationLink 回答'这条管不管用'」一节补记——把它理解成"更严格的可靠性判断"就正好是该节明确反对的"在 verified 之上叠加次数门槛重复验证同一件事"，写代码/写文档时统一按"排序输入，不是准入条件"这个措辞，不要写成"更严格"）。人工手动触发路径（`result_id` 为空）的 qualifying 定义不受影响，仍是 2026-08-07 修订的口径（只要求 `lifecycle=current`）。查询/展示 `activation_links` 时不要把 `status=verified` 直接等同于"已进入 Wiki 材料池"，两者是分开的判断。`page.md` 步骤 2b 已同步新增「Wiki 材料确认队列」视图（`POST /knowledge-points/:id/wiki-material/confirm|reject`），承接原先 candidate 晋升确认队列在系统里的角色——`auto_promote=true` 时那个旧队列的待确认徽标会常年为 0，这是预期行为。**2026-08-12 改判，取代上面 `wiki_material_confirm` 相关的全部口径**：该独立人工确认关卡整体废弃，`wiki.md` 步骤 3 的 qualifying（含 Study 推荐路径）恢复为只看 verified ActivationLink，不再叠加第二道关卡；理由见 `docs/design/wiki.md`「2026-08-12 改判」——脱离具体 Wiki 主题语境，人工看着一条孤立的 KP 判断"值不值得沉淀"并不比程序多掌握信息，真正能做这个判断的时机是 Wiki 编译时（主题范围已定，编译时本来就有的整体判断——广度/连贯/稳定——自然回答了这个问题），所以不是"换一种方式重新加关卡"，而是把这道关卡直接删掉，不设替代机制。`study.qualifying_confirm_success_min`/`qualifying_confirm_distinct_min`、`POST /knowledge-points/:id/wiki-material/confirm|reject`、`learning_results(action=wiki_material_confirm)`、`page.md` 步骤 2b 的确认队列 UI 均已随之删除（代码与 `web/index.html` 已同步移除，未留 auto 开关）。verified 的含义重新收拢为同时表示"够格走快路径"与"够格作为 Wiki 材料"，两件事不再分开判断。**2026-08-13 改判，取代本条以上关于 candidate/verified/weakened/deprecated 离散状态机、`auto_promote`、晋升/降权/重新验证的全部口径**：ActivationLink（连带 ActivationBundle 的触发轴）改用自收敛的连续置信度机制，设计依据 `docs/design/activation-convergence.md`（新文档），已同步改写进 `docs/design/precompile.md`/`activation-bundle.md`/`wiki.md` 等设计文档，以及 `docs/impl/v1/activation.md`/`trace.md`/`study.md`/`retrieval.md`/`activation-bundle.md`/`wiki.md`/`page.md`/`readme.md` 全部工程实现文档。**2026-08-14 更新：实际 Go 代码已按此设计分 6 个阶段全部实现并逐阶段独立验证（`go build`/`go vet`/`go test ./... -count=1` 全绿，Phase 4/5 的并发路径另跑过 `-race`），不再是"仅文档层"**——阶段 1 核心置信度引擎（`internal/activation/confidence.go`，迁移 050，删除离散状态机与 `TransitionLink`/`TransitionBundle`）、阶段 2 Trace 接线（`RecordOutcome`/`RecordAuditOutcome`）、阶段 3 Study 创建门槛+收敛剪枝+趋势报告、阶段 4 Retrieval 分档消费+后台独立核实编排（`AuditOutcomeWriter`）、阶段 5 ActivationBundle 成员置信度轴（迁移 051，`BundleMember`/`RecordMemberOutcome`）与 Wiki 综合满意度轴（迁移 052，纯 audit-only，`RecordSynthesisOutcome`）、阶段 6 `web/index.html` UI（置信度/tier 面板、条件明细表、"清空重来"取代 confirm/reject、Wiki 页面综合满意度只读面板）均已落地。读到这条时以 `docs/impl/v1/activation.md`「状态机」一节与实际代码为准，不要凭这条摘要或更早的旧代码印象；实现计划归档于会话历史（`docs/impl/v1/` 各文档为准绳，未另存独立 plan 文件于仓库内）。核心变化：`activation_links.observed_conditions` 每一条观测条件自带 `success_count`/`failure_count`（+`audited_success_count`/`audited_failure_count`），`mean(cond) = (success_count+1)/(success_count+failure_count+2)`（Beta(success+1, failure+1) 后验均值），按三档服务分档（exploring/self_graded/trusted，配置项 `serving_confidence_min`/`audit_sample_min`/`explore_rate_low`/`explore_rate_self_graded`/`explore_rate_trusted`，已迁到 `retrieval.md` 的 `retrieval:` 节）决定本轮是否直接服务，不再查 `status` 做二元判断；`status` 列保留但改为从条件分数派生的缓存摘要，不是真相源。`auto_promote`/`promote_success_min`/`promote_distinct_min`/`weaken_failure_min`/`weaken_ratio_min`/`reverify_success_min` 六个配置项与 `TransitionLink` 的离散跳变逻辑一并废弃，替代写口径是 `activation.RecordOutcome`（自证）/`RecordAuditOutcome`（独立核实，走新增的 `activation_audit_success`/`activation_audit_failure` Trace 事件，复用既有 `learning_events` 管线，不新建通道）。**创建门槛同步换成同一套 Beta 均值/宽度公式**（`docs/design/activation-convergence.md` 第 11 节）：`study.candidate_confident_min`/`candidate_ratio_min` 废弃，改为 `create_confidence_min`/`create_width_max`，用 `question_kp_cooccurrence` 现成的 `confident_count`/`hit_count` 算 `mean_pre`/`width_pre`，与「服务门槛」「收敛剪枝」用同一套公式、同一种语言——不要三处各自发明一套判定规则；创建门槛刻意设得比服务门槛更松（因为创建后自然由置信度收敛与剪枝兜底，不再需要靠"宁可少建"防错）。ActivationBundle 的两条创建路径处理不同：离线聚类沿用 `distinct_question_count`/`days_active`（多样性/时长信号，不套 Beta 公式，理由见 `activation-convergence.md` 第 11 节）；实时跨 unit 歧义触发路径（`formCandidateBundle`）不加任何门槛，第一次遇到就建，因为触发条件本身（两条已服务/自证链接命中却分属不同 unit）已经是足够高的准入信号。
- **ActivationLink / ActivationBundle 的 Match 改为两级、可调用模型（2026-08-11 新增）**：`activation.md` 步骤 2 的 Match 不再是"纯程序计算，不调用 LLM"——audience/constraint 硬性守门与 subject/intent 精确匹配仍是纯程序、免费，这部分逻辑结构上排在模型之前，模型看不到 audience/constraint 字段、无法覆盖硬性守门；只有"候选组非空、但第一轮程序精确匹配未命中"时才升级为模型辅助判断（第二轮），候选组为空时不触发，避免冷启动/新领域问题也白付一次调用。快路径 LLM 调用预算因此从固定 3 次变成 3~4 次（多数重复问答仍是 3 次），`retrieval.md`「LLM 调用预算对照」与相关完成标准已同步。ActivationBundle 的 Match、以及 Study 显影扫描时"这条新证据算不算同一条已有熟路"的判断，复用同一套两级机制（`activation-bundle.md` 步骤 2/4）——**取代此前"靠归一化四元组分组指纹判断身份"的口径**：分组指纹本身会因四元组解析抖动而不稳定（同 `activation_links` 早年 `UNIQUE(question_terms, point_id)` 的教训），现在改为新证据先尝试 Match 现有熟路，命中即合并，只有全部未命中的证据才会被拿去分组、发现全新熟路；一条熟路一旦显影出来，后续证据都走 Match 吸收，不会再因为解析抖动分裂。**`subject_synonyms` 的 `gap_mined` 挖掘链路定案保留、默认策略改自动（2026-08-12）**：`study.synonym_auto_promote` 默认改为 `true`（原 `false`），理由与 `study.auto_promote` 一致——错误的同义词对最坏只是 Match 第一轮多/少命中一次，`fast_path_verify` 兜底，不直接影响答案正确性，事后可 reject 补救，不需要事前人工逐对审阅。挖掘链路本身**不冻结**：它和模型辅助匹配不是替代关系——模型辅助匹配解决"当场判断"，挖掘链路解决"把判断过的等价关系存下来变成以后免费命中的规则"，后者的价值不因为有前者而消失，反而能降低前者的长期调用量。`activation.md` 步骤 3a 的 `reject` API 范围相应扩大到 `status=active`（此前只能拒绝还没生效的 `candidate`，默认自动生效后大多数行从未经过人工审阅，需要能撤销一条已经在生效的映射）。`page.md` 步骤 2c 补了一个此前完全没有前端入口的浏览/驳回 Tab——这不是新决策带来的缺口，是本来就该有、一直没写的 UI，这次一并补上。**`session_normalize_tuple`（`plan-parser-vocab-and-unit-ambiguity.md` 记录的、已实现的 Match 前二次规范化调用）已废弃（2026-08-12 定案）**——它和上面的第二轮批量匹配解决的是同一个问题，继续保留会在"非首次+域内有词表"场景下为同一件事连续付两次模型调用；新机制覆盖范围更广（不分首次/非首次）、判断方式更直接（不需要先盲改四元组再赌重新匹配）。`retrieval.md` 步骤 2、`activation.md` 步骤 2 已同步；`plan-parser-vocab-and-unit-ambiguity.md` 顶部标了作废说明，其「合并定域+Parse」与「同 KU 歧义」两部分不受影响。**代码层面的实际移除（删 `config/prompts/session_normalize_tuple.md`、去掉 Go 调用点）是本次文档修订之外的后续实现工作，尚未执行**——新机制本身也还没实现，这仍然只是设计决策的文档落地。**2026-08-12 改判（当天晚些时候，取代本条上面关于"两级、可调用模型"的全部口径）**：两级 Match（程序精确匹配 + 候选组非空未命中时升级模型辅助判断）整体撤销，Match 恢复为单级、纯程序、免费——`subject`/`intent`/`audience`/`constraint` 四个字段一律用精确字符串相等判断，不再有任何字段享受同义词归一化/模糊匹配/模型辅助的特殊待遇。根源认识变了：四元组抽取抖动不是 subject 独有的问题，intent/audience/constraint 同样会抖，原先"subject 模糊、其余三项硬 gate"的不对称设计一旦承认这点就站不住；与其把模型辅助匹配再扩展到另外三个字段（调用量滚雪球，重新引入 `session_normalize_tuple` 当初被砍掉的那个成本问题），不如把 Match 整体收回纯精确匹配，把抽取抖动交给别的手段处理（例如收紧抽取阶段的受控词表——另一项尚未排期的独立调查，此处不展开）。`activation_match_judge.md` prompt 已删除，`MatchConditionGroups`/`BuildQueryConditionTerms` 不再接收/使用 `SynonymResolver`，round 2 及相关类型（`matchCandidate`/`nearMissGroups`/`judgeRound2`）已从代码移除，`RetrievalConfig.ActivationMatchModelEnabled` 配置项一并删除。ActivationBundle 的 Match 与 Wiki 四元组直答入口 `matchFourTupleEntry` 复用同一套函数，同步变为纯精确匹配。快路径 LLM 调用预算相应从"3~4 次，多数 3 次"收回到改动前的固定值，见 `retrieval.md`。**以下机制不受本次改判影响，继续保留**：`subject_synonyms` 表/`gap_mined` 挖掘链路（`Matcher.SubjectOnlyMiss`、Trace 的 `subject_synonym_gap` 近似检测）、`preset/domains.json` 别名导入、Wiki 概念页别名展示、`synonym_auto_promote` 默认 `true`、同义词 UI Tab 及其 confirm/reject API——这条挖掘链路解决的是"把判断过的等价关系沉淀成免费规则"，这件事本身不依赖 Match 是否有模型辅助这一环，只是 `SubjectOnlyMiss` 现在改为自行内联同义词归一化比较，不再借用（已经变成纯精确匹配的）`BuildQueryConditionTerms`。
- **ActivationBundle（熟路，2026-08-11 新增，设计方向，尚未排入上面「实现顺序」）**：ActivationLink 只记「单个知识点管不管用」，锚点是 `point_id`；熟路补的是「一组知识点合在一起，对同一类问题管不管用」，锚点是归一化四元组分组，两者共存、不互相替代，单点问题继续走 ActivationLink 最短路径。设计依据 `docs/design/activation-bundle.md`，工程方案见 `docs/impl/v1/activation-bundle.md`（分两阶段：阶段 1 显影/存储/状态机可独立实现，不改变任何现有检索行为；阶段 2 Retrieval 消费 + Trace 回写会改变检索优先级与跨 unit 歧义判定，留给 `retrieval.md`/`trace.md` 各自后续修订，本文档只给契约）。`docs/impl/v1/wiki.md` 步骤 3（qualifying 判定、连贯、内聚）与步骤 8（四元组聚类、候选范围检索、整体可靠度）、`docs/impl/v1/wiki-generation.md`（阶段 A qualifying 定义、阶段 B `w_cooc` 建图公式）、`docs/impl/v1/retrieval.md`（职责总览的检索总流程、步骤 2 的跨 unit 歧义判定；2026-08-12 定案步骤 1 新增 `bundle_hits[]` 字段 `{ bundle_id, member_point_ids[], match_score, matched_by }`，独立于 `activation_hits[]` 不合并，字段形状已定但 Match/候选构建的实际接入仍是「阶段 2」未实现）、`docs/impl/v1/trace.md`（步骤 3 激活类事件生成，预期新增独立的 `bundle_success`/`bundle_failure` 事件；2026-08-12 定案熟路命中不新增 `path_type` 取值，复用 `path_type=fast`，靠 `bundle_hits[]` 是否非空区分来源）、`docs/impl/v1/evidence.md`（职责段熟路指针；2026-08-12 定案熟路候选组装是新增一处调用点，不复用 ActivationLink 现有那处，输入形状不同——挖掘逻辑本身不用改）、`docs/impl/v1/study.md`（步骤 5b，2026-08-12 定案排入调度顺序：步骤 5 之后、步骤 6 之前，不占用现有编号，理由见该步骤说明）、`docs/impl/v1/activation.md`（职责总览，Bundle 存储/匹配器是否封装进本包尚未定案）、`docs/impl/v1/lifecycle.md`（步骤 4，2026-08-12 定案：核心成员 lifecycle 过期 → 熟路立即降权 `verified→weakened`（单次触发，不等窗口）；路肩成员过期不触发状态迁移；熟路成员变化不反向触发 Wiki `needs_recompile`——这条**属于阶段 1 范围，不依赖阶段 2**，其余 Retrieval/Trace 消费仍是阶段 2）、`docs/impl/v1/page.md`（步骤 1「解释抽屉扩展」，`bundle_hits[]` 尚未产生数据，展示方式待阶段 2 落地后再设计；是否需要一个熟路只读浏览视图，2026-08-12 随 `wiki_material_confirm` 队列删除而失去原挂靠位置，尚未定案挂在哪里）都留了「熟路指针」，标注这些位置未来可能吃 ActivationBundle 的信号，**但当前判据/行为均未变**（qualifying 仍是只看 verified ActivationLink 一道关卡——`wiki_material_confirm` 已于 2026-08-12 整体废弃，见上一条——且不吃熟路；连贯仍只数 KPN related 边；内聚的 Louvain 边权仍是临时统计的共享 confident 问题共现，不是复用熟路稳定核；可靠度仍只算 verified 覆盖占比；检索仍是 Wiki 直答 → 单链接 ActivationLink → 慢路径三层，没有熟路层；跨 unit 歧义判定仍对所有命中一视同仁；Trace 仍只产生 activation_success/failure/gap 三种激活类事件，没有 bundle_success/bundle_failure；study.md 文档层面的调度顺序已经定案排入步骤 5b（熟路显影扫描），但代码里 Study 每轮实际跑的仍是步骤 1-7，5b 尚未实现）——不要在实现这几份文档时因为看到指针就自行放宽判据或改动检索/学习流程，具体口径、阈值、是否并入、怎么接入都还需要用户先确认。**2026-08-12 部分落地（当天晚些时候，修正上一句"检索仍是三层、没有熟路层"这个判断的现状部分）**：Retrieval 快路径在 ActivationLink 命中跨 unit 歧义时，会 consult ActivationBundle 作为仲裁——命中一个或多个不冲突（KPN `contradicts` 判定）的 verified Bundle 就合并其核心成员继续走快路径；没有 verified Bundle 覆盖则从这次观测实时新建/加强一条 candidate Bundle（不是 Study 离线聚类），仍回落慢路径（`internal/retrieval/fastpath_helpers.go` `resolveBundleForAmbiguousHits`/`formCandidateBundle`，详见 `activation-bundle.md` 步骤 4 后的修正记录）。这**不是**完整的阶段 2——`bundle_hits[]` 字段、Trace 的 `bundle_success`/`bundle_failure`、Bundle 自己的 `adopt_count`/`known_question_terms`/`auto_promote` 仍未实现，只是"多链接歧义时查/建 Bundle"这一个入口打通了；本条其余部分（qualifying、连贯、内聚、可靠度判据均不吃熟路信号）继续有效不变。**2026-08-20 生成与匹配重新设计（取代上面 2026-08-12 全部关于「按四元组聚类簇显影」「跨 unit 歧义时才 consult」「`formCandidateBundle` 实时新建候选」的口径，设计依据 `docs/design/activation-bundle.md`「10. 改判」）**：根因是离线聚类按 `distinct_question_count`/`days_active` 判断该不该建熟路，衡量的是"问法多不多样"而非"这个组合被反复确证得够不够可靠"——同一句话原样重复问 N 次，`distinct_question_count` 完全不增长，永远建不出熟路；且"两条 Link 各自独立命中同一问题、但从未在同一条 trace 里共同出现过"这种较弱信号，此前被 `formCandidateBundle` 直接当成合并证据来建候选熟路，而它同样可能是互相替代关系（各自单独就够用），不是联合必需。改判要点：**熟路身份从「四元组聚类簇/point 集合」改为「归一化四元组」**（跟 ActivationLink 用同一套 `question_tuple_norms` 归一化空间）——语义相同、成员集合大小不同（如 `{p1,p2}` 与 `{p1,p2,p3}`）自然合并为同一条熟路（成员名单是并集，各自独立置信度决定这次算不算核心），语义不同、碰巧共享部分成员则自然保持独立熟路；**生成门槛复用 ActivationLink 同一套 Beta 均值/宽度公式**（`create_confidence_min`/`create_width_max`，不新增配置），计算对象是"这个归一化四元组本身积累了多少置信证据"（新表 `bundle_trigger_cooccurrence`/`cooccurrence_bundle_dedup`，迁移 061，`internal/study/bundle_scan.go` `scanActivationBundles` 重写，对称于 Link 的 `question_kp_cooccurrence`/`ScanCandidates`）；**唯一可信的生成证据是直接联合引用**（同一条 confident trace 的 `direct_point_ids` 同时出现多个点），"多条 Link 独立命中同一问题"不再直接用于建/合并熟路，只是单纯落回慢路径，`formCandidateBundle`/`sameMemberSet` 已删除；**匹配从"Link 歧义时被动 consult"改为"跟 Link 并行主动匹配"**（`internal/retrieval/service.go` `tryFastPath` 用 `sync.WaitGroup` 并行发起 `activationSvc.Match` 与 `resolveBundleCandidate`，同 Wiki∥ActivationLink 已有的并行写法），命中优先级按连续置信度（tier/mean）比较，不是写死的层级顺序。旧的 `BundleClusterMinQuestions`/`BundleClusterMinDaysActive`/`BundleCoreRatioMin`/`BundleCoreSizeMax` 四个配置项已删除。详见 `docs/impl/v1/activation-bundle.md` 步骤 2/4、`docs/impl/v1/retrieval.md` 步骤 2、`docs/impl/v1/study.md`「归一化四元组累积」。**2026-08-20 补记（当天晚些时候，验证环节接线，代码已实现）**：上面"生成/匹配"重设计之外，"验证"这一环——Bundle 被实时命中服务之后答得对不对，写回触发轴/成员轴——此前完全没有接线：触发轴没有任何 `RecordOutcome` 入口，成员轴的 `RecordMemberOutcome` 虽已存在但生产路径零调用；且即便接上，Study 显影扫描每轮重算时原先是整体覆盖 `member_point_ids`/`observed_conditions`，任何刚累积的实时结果撑不过下一次扫描就被冲掉。这次一并补上：新增 `activation.Service.RecordBundleOutcome`（触发轴，镜像 Link 的 `RecordOutcome`）；成员轴 `RecordMemberOutcome` 补上调用点；`retrieval.EvidenceSet` 新增 `BundleHits` 字段（镜像 `ActivationHits`），把这轮实际命中的 Bundle 及其触发条件四元组、实际用到的成员点位带给 Trace；`trace.generateBundleActivationEvents` 对每个 `BundleHit` 复用 `activation_success`/`activation_failure` 事件类型（不新增 `bundle_success`/`bundle_failure`）写事件并调用上述两个方法；Study 侧显影扫描的重算写入路径从整体覆盖改为合并写（新增 `activation.Store.RefreshBundleMembers`）——已存在的触发条件/成员保留累积的 success/failure 计数，只有新发现的候选才作为新条目插入。详见 `docs/design/activation-bundle.md`「11. 验证环节的实际接线」、`docs/impl/v1/activation-bundle.md` 步骤 5 编注、`docs/impl/v1/trace.md` 步骤 3「Bundle 命中的对称处理」。
- **问题四元组归一化（2026-08-12 新增实现，config-gated，默认关闭）**：三处四元组消费入口（`activation.Matcher`/`BundleMatcher`、`wiki.matchFourTupleEntry`）本身维持纯精确匹配不变；新增的 `activation.TupleNormalizer`（`internal/activation/tuplenorm.go`）在 Retrieval `tryFastPath` 里、送入这三处之前，按三层递进把措辞抖动的四元组替换成第一次见到的 canonical 四元组：Tier 1 精确匹配（`question_tuple_norms` 表，按 `domain_id` 分域）→ Tier 2 本地词集 Jaccard 相似度（`question_tuple_norm_local_sim_min`）→ Tier 3 LLM 批量判断（`config/prompts/tuple_norm_match.md`，audience/constraint 要求比 subject/intent 更严格的等价判断）→ 全部未命中则以当前四元组为新 canonical 记录落库。`QuestionTupleNormEnabled` 默认 `false`，关闭时 `tryFastPath` 行为与改动前完全一致；LLM 调用预算只在双重未命中时 +1 次。**2026-08-20 改判**：曾经的 Tier 2.5 向量早筛（`vector_match_enabled`/`vector_model_dir`/`vector_match_sim_min` 三个配置项、`internal/activation/tuplenorm_vector.go` 的 `VectorEmbedder`/`goformerEmbedder`/`NewGoformerEmbedder`、`TupleNormalizer.SetEmbedder`、`go.mod` 的 `github.com/MichaelAyles/goformer` 依赖）已整体移除，四层收回为三层，代码与 `go.mod`/`go.sum` 已同步删除。question_tuple_norms 的 idle 清理（`question_tuple_norm_idle_days`）跑在 Study 的 `evictIdle` 里，惯例同其余 idle 清理。详见 `docs/impl/v1/retrieval.md` 步骤 2。**2026-08-20 新增第二个调用方**：TupleNormalizer 此前只在查询侧 `tryFastPath` 生效；现在 Study 的 `buildObservedConditions`（`internal/study/service.go`）构建/刷新 `activation_links.observed_conditions` 时也会调用同一个 `activationSvc.NormalizeTuple` 入口，先把 confident trace 的四元组归一化再按四元组分组合并——这是针对"ActivationLink 命中率/复用率低"这个问题给出的根因修复：此前条件按四元组**字面文本**精确分组，同一 KP 被措辞不同但语义等价的问法命中时会各开一条独立 `ObservedCondition`，每条都从零样本起步，置信度永远收敛不上去；归一化让语义等价的问法折叠进同一条条件。两个调用方共享同一张 `question_tuple_norms` 表、同一个 canonical 空间，同一个开关 `retrieval.question_tuple_norm_enabled` 门控，不新增配置项；查询侧 `Matcher`/`BundleMatcher` 的精确匹配算法本身不改。**明确不动**的部分：ActivationLink 的生成门槛（`create_confidence_min`/`create_width_max`）——用户已否决"生成端不设门槛"的方向，理由是会导致候选 Link 爆量不可取。新增 `study.Store.DomainIDsForPoint`（经 `knowledge_points.source_id → sources.domain_id` 反查，喂给 `NormalizeTuple` 按域限定 `question_tuple_norms` 查找范围）。接受时序竞态：构建阶段某问法若还没被查询侧写入过 canonical 记录，本轮就归一化不到、条件没能合并，不做特殊协调，下一轮自然收敛。详见 `docs/impl/v1/study.md`「归一化接入构建阶段」、`docs/impl/v1/activation.md`「条件身份的来源」、`docs/impl/v1/retrieval.md` 步骤 2。

## 技术栈

- Go 1.21+，模块路径：`github.com/jxman78/wiki-brain`（或以 go.mod 实际值为准）
- SQLite：`github.com/mattn/go-sqlite3`
- Bleve：`github.com/blevesearch/bleve/v2` + gse 分词：`github.com/go-ego/gse`、`github.com/go-ego/gse/bleve`
- HTTP：标准库 `net/http`
- 日志：`log/slog`（Go 标准库）
- 前端：单文件 `web/index.html`，`go:embed` 嵌入二进制

## 编码规范

- 包结构：`internal/<module>/`，每模块一个包
- 数据库 migration：`internal/foundation/db/migrations/` 下按版本号命名的 `.sql` 文件
- Prompt 文件：`config/prompts/<用途>.md`，格式见 `docs/impl/mvp/readme.md` Prompt 设计原则节
- 所有 LLM 调用通过 `LLMClient` interface，禁止直接调用 OpenAI SDK
- 测试使用 fake LLM client，不发起真实网络请求
- 错误处理：业务错误返回标准类型（见 foundation.md），不用裸 `errors.New`

## 关键约定

- 位置字段统一用 `line_start` / `line_end`（1-based, inclusive），**禁止** char/byte offset
- 异步队列 task 类型：`source_process` / `unit_extract` / `trace_write`（见 foundation.md）
- EvidenceSet.source_ref 序列化为 JSON 对象 `{"source_id","line_start","line_end"}`
- Study 用 `time.Ticker`，**不走**异步队列
- 预制数据文件：`preset/domains.json`，启动时 UPSERT 写入 domains/entries 表
- JSON Schema 定义在 prompt 文件 `## Schema` 段内，校验的是**程序整合后的结果**，不是模型原始输出；程序负责解析→组装→再校验

## 要求

- 回复时使用和提问时一样的语言；

- 在修改 bug 时，如果不确定问题的根本原因，不要直接修改代码，要和用户确认问题原因；

- 在修改 bug 时，不要破坏原本的设计和实现方案，除非用户确认要修改。

## 参考文档

### MVP（已完成）

| 模块         | 文档                            |
| ---------- | ----------------------------- |
| 总览         | `docs/impl/mvp/readme.md`     |
| Foundation | `docs/impl/mvp/foundation.md` |
| Source     | `docs/impl/mvp/source.md`     |
| Unit       | `docs/impl/mvp/unit.md`       |
| Retrieval  | `docs/impl/mvp/retrieval.md`  |
| Answer     | `docs/impl/mvp/answer.md`     |
| Trace      | `docs/impl/mvp/trace.md`      |
| Study      | `docs/impl/mvp/study.md`      |
| Page       | `docs/impl/mvp/page.md`       |

### V1（进行中，按此顺序实现）

| 顺序 | 模块       | 文档                          |
| -- | -------- | --------------------------- |
| 1  | Lifecycle  | `docs/impl/v1/lifecycle.md` |
| 2  | Activation | `docs/impl/v1/activation.md` |
| 3  | Trace      | `docs/impl/v1/trace.md`     |
| 4  | Study      | `docs/impl/v1/study.md`     |
| 5  | Retrieval  | `docs/impl/v1/retrieval.md` |
| 6  | Evidence   | `docs/impl/v1/evidence.md`  |
| 7  | KPN        | `docs/impl/v1/kpn.md`       |
| 8  | Wiki       | `docs/impl/v1/wiki.md`（编译链路重构见 `docs/impl/v1/wiki-generation.md`：P0 已实现；P1 切面聚类已实现，写作调用维持两次整页 LLM 调用（analyze+compile，材料按切面分组），**不做**提纲/逐节生成——该架构曾实现过一版又确认收缩，代码收缩指令见 `docs/impl/v1/wiki-generation-simplify-task-brief.md`。**2026-08-18 单层化改判已实施**，取代 `wiki.md` 中两层架构相关章节，以 `docs/design/wiki-single-tier-revision.md` + `docs/impl/v1/wiki-single-tier-task-brief.md` 为准，详见上方「V1 关键设计决策」）      |
| 9  | Page       | `docs/impl/v1/page.md`      |

设计方向（不在上面强制顺序内，尚未排期，见文档内「待确认」标注）：`docs/impl/v1/activation-bundle.md`（ActivationBundle / 熟路——ActivationLink 之上的组合激活层）。

总览与范围边界：`docs/impl/v1/readme.md`；设计依据：`docs/design/lifecycle.md`（记忆生命周期）、`docs/design/precompile.md`（ActivationLink）、`docs/design/activation-bundle.md`（ActivationBundle / 熟路）、`docs/design/study.md`（学习机制）、`docs/design/retrieval.md`（分层检索）、`docs/design/wiki.md`（Wiki 编译）。
